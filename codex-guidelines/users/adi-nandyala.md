# User Context: adi.nandyala@cloudfuze.com

> Derived from Claude conversation history via ccmigrate.
> Source: 16 conversations

## Work Context from Conversations

### IoT filter health monitoring app

Here's your updated prompt for Claude Code:

---

**Build a mobile app called "SeasBeforeSpin" using Expo (React Native). Use only dummy/mock data. It's a dashboard for monitoring a DIY laundry microfiber filter.**

**Tech Stack:**
- Expo (React Native)
- expo-router for navigation
- react-native-svg and victory-native for charts
- @expo/vector-icons for icons
- Linear gradient via expo-linear-gradient
- All dummy/hardcoded data, no backend or IoT needed

---

**Screens:**

**1. Dashboard Screen (index)**
- Large circular SVG progress ring showing **Filter Health: 62%**
- Color coding: Green (70–100%), Yellow (40–69%), Red (0–39%)
- Status message: *"Filter needs cleaning soon"* at 62%
- Stat cards in a 2x2 grid showing:
  - Flow Rate: 2.3 L/min
  - Particles Detected: 14,200
  - Turbidity

### Comprehensive app development prompt with exceeds expectations rubric

The Instagram Graph API gives you a few features that most competing teams won't think to implement beyond just showing a feed. Here's what would really stand out:

**Instagram Stories Integration**

Most teams will pull posts. Almost nobody will pull Stories. The Instagram Graph API lets you fetch a business account's active Stories via the `/stories` endpoint. You could build a Stories-style carousel at the top of your Social Hub — the same horizontal circles UI that Instagram itself uses. Members tap a circle, and it shows the chapter's current Stories in a full-screen viewer with auto-advance. This is visually impressive during a demo and judges will immediately recognize the pattern.

**What to say to judges:** "We don't just pull static posts — we fetch the chapter's live Instagram S

### Building an IoT filter monitoring app

Here are your staged prompts for Claude Code. Copy and paste each one in order — finish each stage before moving to the next.

---

## Stage 1 — Project Scaffold & Navigation
```
Create a new React Native + Expo app called "FilterCycle". Set up the following:

- Use Expo Router for file-based navigation with a bottom tab navigator
- 4 tabs: Dashboard, History, Filter Health, Alerts
- Dark mode theme throughout: background #0A0A0F, card surfaces #13131A, accent color #00C9A7 (teal-green), text white and #8A8A9A for secondary
- Use a custom tab bar with icons (use @expo/vector-icons with Ionicons)
- Create a global theme file (theme.ts) with all colors, font sizes, spacing constants
- Install and configure: recharts (or victory-native for charts), expo-linear-gradient, react-native-safe-area

### Finding API keys

For Claude/Anthropic API keys, you can find them at **console.anthropic.com** — navigate to "API Keys" in the settings or dashboard after signing in.

For more details on getting started with the API, check out **docs.claude.com**.

### Commercial property investment analysis for NC

Let me try again — looks like the permissions may have refreshed.
```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```

The extension still has partial access — navigation and page text reading work, but screenshots, clicks, and JS are blocked. Let me try creating a fresh tab — that sometimes resolves permission issues.
```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```

Same issue on the new tab. The extension has a core permissions problem 